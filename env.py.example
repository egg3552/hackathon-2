"""
Example env.py file for local development
Copy this to env.py and update with your actual values
NEVER commit env.py to GitHub!
"""

import os

# Required for local development
os.environ.setdefault('SECRET_KEY', 'your-secret-key-here')
os.environ.setdefault('DEBUG', 'True')

# Optional: For testing with PostgreSQL locally
# os.environ.setdefault('DATABASE_URL', 'postgres://user:pass@localhost/dbname')
